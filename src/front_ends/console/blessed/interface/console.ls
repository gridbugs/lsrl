define [
    'interface/console'
], (BaseConsole) ->

    class Console extends BaseConsole
        ->

        print: ->
        newLine: ->
        clearLine: ->
        readString: ->
        readInteger: ->
